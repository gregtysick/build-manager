# Skill Package Notes

This folder is reserved for the publishable skill-package material that will be cleaned up and copied from the live local OpenClaw skill/runtime once the repo packaging boundary is tighter.

It is intentionally light right now so the repository can start documentation-first without dragging in local runtime state.
